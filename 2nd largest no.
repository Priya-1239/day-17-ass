#include <stdio.h>

int main()

{

int i, m1, m2, n, num;

printf("Enter how many numbers"); scanf("%d",&n);

for (i=0;i<n;i++)
{
scanf("%d",&num); 
if(i==0)
{
num=m1=m2;
}
if (num>m1)
{
m2 = m1;
m1 = num;
}
else if(num>m2)
{
m2=num;
}
}
printf("Second largest number is %d", m2);
return 0;
}
